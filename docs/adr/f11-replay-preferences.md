# ADR：F11 回放使用当前权限，记忆仅保存确认偏好

- 状态：Accepted（用户已确认），2026-09-21。
- 决策：持久化规范结果；公开引用每次重新授权并签发。撤权保留原正文与状态，隐藏引用，不自动重答。
- 附件：G=[] 不等于合法附件为空。复用官方文件 API 与 solo 生成，禁止扩大 KB 范围；生产 Workflow 保持现有分支。
- 记忆：有限表达偏好 + 显式用户确认；企业事务保存确认值和 outbox；官方 Memory 为非权威镜像。拒绝把历史技术事实作为个人记忆注入。
- 取舍：增加企业表和确认操作，避免权限/版本变化后的事实泄漏；无新的模型抽取调用。官方 API 缺幂等接收，只做有界重试，不能宣称 exactly-once。
- 测试、上线、回滚与上游重放见 [变更登记](../../patches/CHANGE-REQUEST-F11-REPLAY-PREFERENCES.md)。ACL/F10 保持独立任务。
