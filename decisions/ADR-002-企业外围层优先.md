# ADR-002 企业外围层优先

- 状态：Accepted
- 日期：2026-08-04

## 决策
客户专属身份、ACL、文件生命周期、业务 PostgreSQL 查询和业务前端放入 `enterprise/`，不直接塞入 RAGFlow 核心。

## 原因
- 客户规则变化快且与 RAGFlow 通用能力无关；
- 可通过公开 API 形成防腐层；
- 减少升级冲突；
- 便于独立测试、审计和安全控制。

## 例外
只有公开 API 无法实现且业务为 P0 阻断时，才允许最小核心补丁，并创建独立 ADR 和 patch manifest。
