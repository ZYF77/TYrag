# WP-05 业务化前端

## Owner
Frontend Agent

## 目标
实现普通用户问答、历史、设备上下文、引用抽屉、PDF/图片/表格证据，以及维护员同步和复核页面。

## 依赖
依赖冻结 OpenAPI；后端未完成时使用 MSW/Mock。

## 交付物
- `enterprise/web`；
- 角色路由；
- SSE 客户端；
- 引用和文件预览；
- 状态/错误展示；
- E2E。

## 验收
- 普通用户看不到高级配置；
- 401/403/404 不泄漏资源；
- 引用打开前重新鉴权；
- 真实状态不伪造百分比；
- SSE 断连正确。

## 禁止修改/禁止范围
不得把 API Key、对象键或管理凭据放浏览器；不得以隐藏按钮替代权限；不得直接调用内部数据库服务。

## 完成报告
列出修改文件、测试命令、结果、契约变化、上游补丁、风险和后续集成注意事项。

## 最小真实联调启动记录（无密钥）

真实联调模式通过环境变量切换，仓库不提交 JWT、RAGFlow API Key、MinIO 密钥或本地运行文件。

### Enterprise Web

前置条件：RAGFlow 与 Gateway 已在本地运行。

```powershell
$env:VITE_API_MODE = "demo"
$env:VITE_DEMO_JWT = "<运行期注入的本地测试 JWT，不写入源码>"
$env:VITE_DEMO_EXTERNAL_DOCUMENT_ID = "E2E-Doc1"
node node_modules/vite/bin/vite.js
```

环境变量说明：

- `VITE_API_MODE`：`mock|demo|gateway`。`mock` 为默认测试模式；`demo` 使用真实 Gateway demo adapter；`gateway` 预留正式 Gateway adapter。
- `VITE_DEMO_JWT`：运行期 Bearer JWT；仅在 demo adapter 读取，不进入源码、测试快照或日志。
- `VITE_DEMO_EXTERNAL_DOCUMENT_ID`：联调用 externalDocumentId。

### 后端与中间件

- RAGFlow：`cd ragflow/docker; docker compose --profile cpu up -d`。
- Gateway：进入 `enterprise/gateway`，按该目录现有配置启动；密钥从运行环境加载。

页面组件不直接调用 `/demo/*`，demo API 调用集中在 `enterprise/web/src/api/demoClient.ts`。
