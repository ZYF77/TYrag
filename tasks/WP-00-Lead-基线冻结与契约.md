# WP-00 Lead：基线冻结与共享契约

## Owner
Lead Agent（串行，第一优先）

## 目标
固定 RAGFlow 版本并证明源码、镜像、入口脚本、迁移和运行时一致；创建 `enterprise/` 脚手架；验证真实 PDF POC；冻结 OpenAPI、metadata、状态和错误码。

## 依赖
无。必须先完成，其他工作包才能进入代码合并。

## 交付物
- version manifest 和启动检查；
- 企业目录脚手架；
- RAGFlow API 能力矩阵；
- 测试 Compose/fixture；
- 共享契约 v1；
- 是否需要上游补丁的 ADR。

## 验收
- 固定版本一条命令启动；
- 数字/扫描/混合 PDF POC；
- API、DB、文档引擎和对象存储 ready；
- 契约 lint 通过；
- 无敏感配置提交。

## 禁止修改/禁止范围
不得开始业务功能大规模开发；不得替换数据库或文档引擎；不得使用 nightly 作为生产基线。

## 完成报告
列出修改文件、测试命令、结果、契约变化、上游补丁、风险和后续集成注意事项。
